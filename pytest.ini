[pytest]
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:backend.*
markers =
    concurrency: exercises two simultaneous transactions against the database
